{
    "to find multiplicative inverse for a non prime modulo": {
    "prefix": "modulo_inverse",
    "body": [
      "int Find_mod_Inverse(int a, int m) {",
      "  for (int i = 1; i < m; i++) {",
      "    if (((a % m) * (i % m)) % m == 1) {",
      "      return i;",
      "    }",
      "  }",
      "  return -1;",
      "}"
    ],
    "description": "to find multiplicative inverse for a non prime modulo"
  }
}